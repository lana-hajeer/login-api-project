# login-api-project
A secure Java Spring Boot REST API for user authentication and login management. Built with Maven, it processes JSON POST requests at /api/login to validate credentials, mapping payloads to custom models with verified HTTP 200 responses.
